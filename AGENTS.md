# Project Context
This is an assignment project for Backend AI Engineering. 

# Tech Stack
Python 3.10+, FastAPI, and Pydantic.

# Rules
* Never use `any` (if using TypeScript).
* Always write a unit test for new features.
* Never overwrite existing comments.
* Write clean and modular code.

# Workflow
* Before writing code, read the active task brief in `docs/tasks/`.
* Follow the commands outlined in `docs/ai/commands.md` for testing and running the project.
